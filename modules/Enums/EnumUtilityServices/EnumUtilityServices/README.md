# Description
Utility class about enum.

# Features
## 1.0.0
### Added
+ Utility class to more easily get entries of enum

NOT ONLY supports classic way (use `String`) to get the description.
BUT ALSO supports modern way (use `Span`) to get the description which has better performance.

## 1.1.0
### Added
+ get enum names
