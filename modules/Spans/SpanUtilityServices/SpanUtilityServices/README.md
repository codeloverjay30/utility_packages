# Description
Utility class to quickly check an instance is in type of `Span<T>` etc.

See [Needs](..\Attachments\需求書.docx) for more details.

# Features
## 1.0.0-preview-1.0.0
### Added
+ quickly check an instance is in type of `Span<T>` etc.

See [Needs](..\Attachments\需求書.docx) for more details.

## 1.1.0-preview-1.0.0
### Added
+ quickly check an instance is empty or not (if it is in type of `Span<T>`)

## 2.0.0-preview-1.0.0
### Fixed
+ Bugs occured when quickly check an instance is empty or not (if it is in type of `Span<T>`)

## 3.0.0-preview-1.0.0
### Fixed
+ Bugs occured when passing a unmanaged heap.