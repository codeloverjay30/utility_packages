# Description
Utility package to get value from registry table in Windows

# Features
## 1.0.0-preview-1.0.0
### Added
+ get value from registry table in Windows

> [!WARNING]
> This is only supported on Windows as the registry table only exists on Windows.