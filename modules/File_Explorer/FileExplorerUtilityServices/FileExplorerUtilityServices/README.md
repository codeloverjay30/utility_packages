# Description
Utility class to auto refresh the icon of `Task bar` and `Quick Access bar` once after locking one or more `Drive(s)`

# Features
## 1.0.0-preview-1.0.0
### Added
+ auto refresh the icon of `Task bar` and `Quick Access bar` once after locking one or more `Drive(s)`