/**
 * Enumeration for extension actions.
 * Represents the possible actions that can be determined for an extension based on the configuration.
 */
export enum ExtensionAction {
    Install = 'Install',
    Enable = 'Enable',
    Disable = 'Disable',
    Ignore = 'Ignore',
    Default = 'Default'
}