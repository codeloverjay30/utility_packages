# Description
utility service to iterate all directories under specific directory to execute `git` commands.

# Features
## 1.0.0    
### Added
+ to execute `git` commands.

+ get difference by `git` -- `git diff`

## 1.1.0
### Added
